<!-- Begin Header-->
<%- include('../../partials/header.ejs') %>
<!-- End of Header-->

<!-- Page Wrapper -->
<div id="wrapper">
  <!-- Begin Sidebar-->
  <%- include('../../partials/sidebar.ejs') %>
  <!-- End of Sidebar-->

  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">
    <!-- Main Content -->
    <div id="content">
      <!-- Begin Navbar-->
      <%- include('../../partials/navbar.ejs') %>
      <!-- End of Navbar-->

      <!-- Begin Page Content -->
      <div class="container-fluid">
        <%- include('../../partials/message.ejs') %>

        <!-- Page Heading -->
        <h1 class="h3 mb-4 text-gray-800">LAVAKBDG | Pelapor</h1>
        <!-- End of Heading-->

        
        <ul class="nav nav-tabs" id="myTab" role="tablist">
          <li class="nav-item">
            <a
              class="nav-link active"
              id="home-tab"
              data-toggle="tab"
              href="#show-pelapor"
              role="tab"
              aria-controls="home"
              aria-selected="true"
              >Tampilkan Pelapor</a
            >
          </li>
          <li class="nav-item">
            <a
              class="nav-link"
              id="profile-tab"
              data-toggle="tab"
              href="#add-pelapor"
              role="tab"
              aria-controls="profile"
              aria-selected="false"
              >Tambah Pelapor</a
            >
          </li>
        </ul>
        <div class="tab-content" id="myTabContent">
          <%- include('show_table_pelapor.ejs') %>
          <%- include('add_pelapor.ejs') %>
        </div>

      </div>
      <!-- /.container-fluid -->
    </div>
    <!-- End of Main Content -->

    <!-- Footer -->
    <%- include('../../partials/footer.ejs') %>
    <!-- End of Footer -->
  </div>
  <!-- End of Content Wrapper -->
</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div
  class="modal fade"
  id="logoutModal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
        <button
          class="close"
          type="button"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body">
        Select "Logout" below if you are ready to end your current session.
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" type="button" data-dismiss="modal">
          Cancel
        </button>
        <a class="btn btn-primary" href="login.html">Logout</a>
      </div>
    </div>
  </div>
</div>

<!-- Script (JS)-->

<%- include('../../partials/js.ejs') %>
<%- include('view_modal.ejs') %>

<script>
  $('#dataTable').on('click', '.button-view', function(){
    //pastikan class="form control" di view modal
    //let variabel = $(this).data('data-'), data- letak di show table
    //$(.name).val(variabel), name letak di view modal
    let id = $(this).data('id'); 
    let nohp = $(this).data('nohp');
    let nama = $(this).data('nama');
    let usia = $(this).data('usia');
    let jenisKelamin = $(this).data('jeniskelamin');

    let tglvaksin1 = $(this).data('tglvaksin1');
    let tmptvaksin1 = $(this).data('tmptvaksin1');
    let categoryId1 = $(this).data('categoryId1');
    let categoryid1name = $(this).data('categoryid1name'); //ini
    let about1 = $(this).data('about1');

    let tglvaksin2 = $(this).data('tglvaksin2');
    let tmptvaksin2 = $(this).data('tmptvaksin2');
    let categoryId2 = $(this).data('categoryId2');
    let categoryid2name = $(this).data('categoryid2name');
    let about2 = $(this).data('about2');

    let about = $(this).data('about');

    console.log(categoryId1);
   
    console.log(id, nohp,
        nama,
        jenisKelamin,
        usia,
        tglvaksin1,
        categoryid1name,
        categoryid2name,
        tmptvaksin1);

    $('#view-modal').modal('show');
    $('.id').val(id);
    $('.nohp').val(nohp);
    $('.nama').val(nama);
    $('.usia').val(usia);
    $('.jenisKelamin').val(jenisKelamin);

    //Harus ada first-child dulu untuk load 

    $('select[name=category1] > option:first-child') 
    .text(categoryid1name);

    $('select[name=category2] > option:first-child')
    .text(categoryid2name);

    $('.tglvaksin1').val(tglvaksin1);
    $('.categoryid1').val(categoryid1name);
    $('.tmptvaksin1').val(tmptvaksin1);
    $('.about1').val(about1);


    $('.tglvaksin2').val(tglvaksin2);
    $('.categoryid2').val(categoryid2name);
    $('.tmptvaksin2').val(tmptvaksin2);
    $('.about2').val(about2);

    $('.about').val(about);





  })
  CKEDITOR.replace("about");
  CKEDITOR.replace("about1");
  CKEDITOR.replace("about2");
</script>
